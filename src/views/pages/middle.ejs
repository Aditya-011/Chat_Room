<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chat Room</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Lato&display=swap");

      * {
        padding: 0;
        font-family: "Lato", sans-serif;
        margin: 0;
      }
      button:focus,
      button:active {
        outline: none;
      }
      .wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 99.99vh;
        width: 100%;
        background-color: rgba(0, 0, 0, 0.2);
      }
      .cover {
        background-color: #fff;
        width: 70vw;
        height: 27rem;
        border-radius: 10px;
      }
      .close {
        display: flex;
        justify-content: flex-end;
        padding: 0.8rem 1rem;
      }
      .close button {
        padding: 0.4rem 0.6rem;
        background-color: #482ff7;
        color: #ffffff;
        font-size: 1.1rem;
        border: none;
        border-radius: 2px;
        cursor: pointer;
      }
      .enterBtn {
        padding: 0.4rem 0.6rem;
        background-color: #482ff7;
        color: #ffffff;
        font-size: 1.1rem;
        border: none;
        border-radius: 2px;
        cursor: pointer;
      }
      .enterBtnCover {
        margin-top: 2rem;
        display: flex;
        justify-content: center;
      }
      .pic {
        display: flex;
        justify-content: center;
        margin-top: 1rem;
        border: none;
        border-radius: 50%;
      }
      .name {
        text-align: center;
        font-size: 1.5rem;
        margin-top: 1rem;
      }
      .email {
        margin-top: 1rem;
        text-align: center;
      }
      .welcome {
        font-size: 1.5rem;
        margin-top: 1rem;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="cover">
        <div class="close">
          <a href="/logout"><button>Log Out</button></a>
        </div>
        <div class="pic">
          <img src="<%= pic %>" width="100" height="100" alt="pic" />
        </div>
        <div class="welcome">
          <h1>Welcome</h1>
        </div>
        <div class="name">
          <h2><%= name %></h2>
        </div>
        <div class="email">
          <h4><%= email %></h4>
        </div>
        <div class="enterBtnCover">
          <button class="enterBtn"><a href="/enter">Enter Chat Room</a></button>
        </div>
      </div>
    </div>
  </body>
</html>
